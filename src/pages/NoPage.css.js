import { style } from '@vanilla-extract/css'

export const noPageContainerStyle = style({
    marginTop: '50px',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
})

export const noPageIconStyle = style({
    marginBottom: '25px',
})

export const noPageHeaderStyle = style({
    marginTop: '25%',
})

export const noPageSubheaderStyle = style({
    marginTop: '20px',
})
